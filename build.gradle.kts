// Versions are pinned here at the root so the whole project builds the same
// way on every machine. Your track's checkpoints will grow this, but the
// starter keeps it to exactly what three screens and one fetch need.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
    id("org.jetbrains.kotlin.plugin.compose") version "2.0.21" apply false
}
